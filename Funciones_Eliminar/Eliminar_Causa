hice esta funcion pero se me ocurrio manejarlo como menu directamente, con la opcion de eliminar TODA la causa, y tambien modularmente dato por dato. 




void eliminarCausa(struct nodoCausas **listaCausa, char *ruc) {
    if (listaCausa == NULL) {
        printf("No se han encontrado causas");
        return;
    }

    struct nodoCausas *actual = *listaCausa;

    do {
        if (strcmp(actual->datosCausa->ruc, ruc) == 0) {
            reenlazarNodoCircularDoble(listaCausa, actual);
            liberarCausa(actual->datosCausa);
            free(actual);
            printf("Causa eliminada correctamente.\n");
            return;
        }
        actual = actual->siguiente;
    }while (actual != *listaCausa);
};
